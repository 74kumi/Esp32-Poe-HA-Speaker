from pathlib import Path
import json,shutil
D=Path('KiCad-RevE');E=Path('engineering');d=json.loads((D/'reports/power-trial-drc.json').read_text());assert not d['violations'] and not d['unconnected_items'];backup=D/'before-load-neck-widths.kicad_pcb';assert not backup.exists();shutil.copy2(D/'PoE-Speaker-RevE.kicad_pcb',backup);shutil.copy2(D/'power-trial.kicad_pcb',D/'PoE-Speaker-RevE.kicad_pcb')
with (E/'REVE-TRUNK-ROUTING.md').open('a',encoding='utf-8') as f:f.write('\n\n## Accepted remaining load-neck widening — 2026-09-14\n\nFour audited load-path segments widened to 0.6 mm: 24V_OR 4ad3da69 and 5075d3e5; 24V_AMP 44438f23; 5V 613a3492 (full UUIDs and original widths in reports/load-neck-width-trial.json). Trial has zero DRC findings and zero unconnected items. The 0.5 mm-long U16 output escape de68fccd remains 0.3 mm: widening to 0.6 mm reduced clearance to AMP_SDZ pin 16 to 0.075 mm, so that change was reverted. No component or netlist changes. These widths do not establish via ampacity, temperature rise, or return-path quality. Backup: before-load-neck-widths.kicad_pcb.\n')
f=E/'package_prototype_review.py';s=f.read_text();s=s.replace('Next: continue amplifier supply/ground-return review using reports/amplifier-decoupling-audit.json.', 'Next: review load-path vias and amplifier supply/ground returns. Four remaining load-neck segments are now 0.6 mm; the 0.3 mm wide, 0.5 mm long U16 output pin escape remains to preserve adjacent-pad clearance. See reports/load-neck-width-trial.json and reports/supply-path-audit.json. Continue amplifier review using reports/amplifier-decoupling-audit.json.');f.write_text(s)
