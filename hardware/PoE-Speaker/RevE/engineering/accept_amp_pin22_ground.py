from pathlib import Path
import json,pcbnew as p,shutil
D=Path('KiCad-RevE');E=Path('engineering');b=p.LoadBoard(str(D/'power-trial.kicad_pcb'));pos=p.VECTOR2I(p.FromMM(209.88),p.FromMM(112.453));z=next(z for z in b.Zones() if z.GetNetname()=='GND' and z.GetLayer()==p.In1_Cu);assert z.HitTestFilledArea(p.In1_Cu,pos);d=json.loads((D/'reports/power-trial-drc.json').read_text());assert not d['violations'] and not d['unconnected_items'];backup=D/'before-pin22-ground-via.kicad_pcb';assert not backup.exists();shutil.copy2(D/'PoE-Speaker-RevE.kicad_pcb',backup);shutil.copy2(D/'power-trial.kicad_pcb',D/'PoE-Speaker-RevE.kicad_pcb')
with (E/'REVE-TRUNK-ROUTING.md').open('a',encoding='utf-8') as f:f.write('\n\n## Accepted U5 pin 22 ground-plane access — 2026-09-14\n\nAdded a 0.6 mm copper / 0.3 mm drill GND via at (209.88,112.453) on the existing ground track. Via center contacts filled In1.Cu GND; pad-hole clearance screened with a 0.25 mm center margin. First trial at (210.822,112.625) conflicted with AMP_SW_POS on B.Cu and was rejected. Accepted position has zero DRC and unconnected findings. No old ground routes removed. Backup before-pin22-ground-via.kicad_pcb. Added audit_amp_ground_plane.py to locate shortest whole-segment copper paths from all ten ground pins to filled-plane-contact vias or plated pads; this does not qualify return impedance or uninterrupted current corridors.\n')
f=E/'checkpoint_reve.py';s=f.read_text().replace("'audit_amplifier_ground.py',", "'audit_amplifier_ground.py','audit_amp_ground_plane.py',");f.write_text(s)
f=E/'package_prototype_review.py';s=f.read_text().replace('Next: verify downstream amplifier ground-pin-to-plane paths and ground-plane continuity.', 'Next: review remaining amplifier ground-pin-to-plane paths using reports/amp-ground-plane-paths.json, especially pin 1 and the left-side ground groups. A new via at (209.88,112.453) gives pin 22 local filled-plane access; downstream return impedance and continuous current corridors remain unqualified.');f.write_text(s)
